# تقرير تحديثات الدورة الشهرية والحضور وتطبيق الموبايل

## ما تم تنفيذه

1. **تصحيح دورة الشهر**
   - تم تثبيت حساب الدورة من يوم 26 حتى نهاية يوم 25.
   - تم تعديل دالة التاريخ حتى لا تتأثر بفروق التوقيت عند استخدام `toISOString`.

2. **إصلاح تسجيل الحضور والانصراف**
   - تم تعديل كود الحضور بحيث لا يعتمد على `upsert` فقط، لأن بعض قواعد Supabase تفشل مع `onConflict`.
   - الآن التطبيق يبحث عن حضور اليوم ثم يعمل insert أو update بشكل واضح.
   - تم تجهيز جدول `attendance` وصلاحياته في SQL رقم 32.

3. **تقرير PDF نهاية الدورة**
   - تم تطوير تقرير نهاية الدورة في صفحة `/admin/reconciliation`.
   - التقرير يحتوي على:
     - اسم المندوب.
     - الفرع.
     - عدد أيام الحضور.
     - عدد ساعات الحضور.
     - عدد الأذونات.
     - حافز البداية.
     - إجمالي الخصومات المعتمدة.
     - إجمالي المكافآت المعتمدة.
     - الحافز بعد الخصومات والمكافآت.
     - إجمالي الأوردرات.
     - أوردرات ×1.
     - أوردرات ×1.5.
     - وحدات الأوردرات.
     - عدد المشاوير.
     - الفاشلة/الخاطئة.
     - المكررة/قيد المراجعة.
     - المحذوفة.
     - تفاصيل الخصومات والمكافآت باليوم والسبب.

4. **بنود الخصومات والمكافآت**
   - أضيف جدول `rider_action_rules` في SQL رقم 32.
   - يحتوي على بنود خصم واضحة مثل التأخير، فشل أوردر بسبب الدليفري، مشوار غير مثبت، تكرار فاتورة بدون سبب.
   - يحتوي على بنود مكافآت مثل شيفت ممتاز، أداء عالي، إشادة عميل، مساعدة الفريق.

5. **حافز الدليفري**
   - التقرير يعتمد على `monthly_incentive_base` أو `monthly_bonus_base` لكل مندوب.
   - الحافز النهائي في التقرير = حافز البداية - الخصومات المعتمدة + المكافآت المعتمدة.

6. **التطبيق على الموبايل**
   - تم تفعيل PWA في Vite.
   - التطبيق ينتج `manifest.webmanifest` و `service worker`.
   - يمكن إضافته من المتصفح على الموبايل إلى الشاشة الرئيسية بعد نشر Vercel.

## ملفات مهمة

- `supabase/32_monthly_pdf_attendance_incentives_pwa_fix.sql`
- `src/lib/helpers.ts`
- `src/pages/rider/RiderDashboard.tsx`
- `src/pages/admin/Reconciliation.tsx`
- `vite.config.ts`

## الاختبار

تم تشغيل:

```bash
npm run typecheck
npm run build
```

والاتنين نجحوا.
