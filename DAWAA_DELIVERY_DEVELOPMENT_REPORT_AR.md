# تقرير تطوير تطبيق دليفري Dawaa Pharmacy

## ملخص التنفيذ
تم تطوير نسخة أقوى من تطبيق الدليفري بناءً على مراجعة الملف المرفوع، مع التركيز على منع التلاعب، جعل تسجيل الأوردرات والحضور أكثر أمانًا، إضافة رفع العملاء اليومي، إضافة تحليل العملاء، وتجهيز Migration آمن لتحسين Supabase بدون حذف بيانات.

## ما تم تعديله فعليًا

### 1) قفل أخطر باب للتلاعب في شاشة الدليفري
- تم إلغاء مسار تسجيل الأوردر المباشر في `delivery_orders` من شاشة الدليفري عند فشل RPC.
- أصبح تسجيل الأوردر يتم من خلال `rider_create_order` فقط.
- لو الجلسة غير موجودة أو الـ RPC رفض العملية، لا يتم حفظ الأوردر بديلًا بطريقة غير آمنة.

### 2) إلزام صورة الريسيت قبل تسجيل الأوردر
- تم منع حفظ أي أوردر بدون صورة ريسيت.
- صورة الريسيت يتم رفعها قبل استدعاء RPC، ويتم إرسال مسار الصورة ورابطها للـ RPC.
- نتيجة OCR إن وجدت يتم إرسالها أيضًا لقاعدة البيانات.

### 3) إلزام GPS في الحضور والانصراف
- تم تعديل تسجيل الحضور والانصراف ليطلب GPS من الجهاز.
- يتم إرسال `lat/lng/accuracy` إلى `rider_check_in_out`.
- لو دقة GPS أكبر من 100 متر، التطبيق يظهر تنبيه أن العملية قد تحتاج مراجعة.

### 4) إلزام GPS في تسجيل الأوردر
- قبل تسجيل الأوردر، التطبيق يطلب موقع الدليفري.
- يتم إرسال الموقع ودقته إلى RPC.
- هذا يساعد لاحقًا في كشف الأوردرات المشبوهة أو التسجيل من مكان غير منطقي.

### 5) إضافة صفحة رفع العملاء اليومي
المسار الجديد:
`/admin/customer-import`

الصفحة تدعم:
- رفع Excel / CSV.
- قراءة أول Sheet من الملف.
- التعرف على أسماء الأعمدة العربية والإنجليزية.
- تنظيف رقم الهاتف المصري.
- Preview قبل الحفظ.
- حفظ العملاء الجدد وتحديث العملاء الموجودين.
- تسجيل Batch لكل عملية رفع.
- تسجيل الصفوف التي بها أخطاء في `customer_import_errors`.

### 6) إضافة صفحة تحليل العملاء
المسار الجديد:
`/admin/customer-analytics`

الصفحة تعرض:
- إجمالي العملاء.
- عملاء VIP.
- العملاء المتوقفين أو المعرضين للتوقف.
- العملاء أصحاب مشاكل توصيل.
- آخر شراء.
- عدد أوردرات التوصيل.
- عدد الأوردرات المطابقة.
- تصنيف العميل ومستوى الخطورة.

### 7) إضافة روابط الصفحات الجديدة في لوحة الإدارة
تمت إضافة:
- رفع العملاء.
- تحليل العملاء.

إلى القائمة الرئيسية في Admin Dashboard.

### 8) تحديث الصلاحيات
تمت إضافة مفاتيح صلاحيات جديدة:
- `customer_import`
- `customer_analytics`

وتم السماح بها للأدمن ومدير الفرع ضمن نظام الصلاحيات الحالي.

### 9) إضافة Migration جديد لـ Supabase
الملف الجديد:
`supabase/52_delivery_intelligence_hardening.sql`

يشمل:
- دوال دورة الشهر من يوم 26 إلى يوم 25.
- جدول `database_deprecation_registry` لتسجيل الجداول المرشحة للأرشفة بدل حذفها.
- جداول رفع العملاء:
  - `customer_import_batches`
  - `customer_import_errors`
  - `customer_daily_snapshots`
- جداول رفع فواتير المبيعات:
  - `delivery_sales_invoice_import_batches`
  - `delivery_sales_invoice_import_errors`
  - `delivery_sales_invoices`
- أعمدة إضافية آمنة في `delivery_customers`.
- أعمدة إثبات ومراجعة في `delivery_orders`.
- أعمدة GPS في `attendance` و `delivery_attendance` إن وجدت.
- Indexes لتحسين الأداء.
- View لتحليل العملاء: `customer_delivery_analytics`.
- View لحوافز الدليفري الحالية: `rider_incentive_current_cycle_view`.
- RPC محسّنة:
  - `rider_check_in_out`
  - `rider_create_order`

## اختبارات تمت
تم تشغيل:

```bash
npm run typecheck
npm run build
```

والنتيجة: نجح البناء بدون أخطاء TypeScript أو Build.

## الشكل العام المقترح بعد التطوير

### شاشة الدليفري
- تسجيل دخول PIN.
- جهاز واحد معتمد.
- حضور/انصراف بزر واضح.
- GPS إجباري.
- تسجيل أوردر بخطوات بسيطة:
  1. اختيار العميل أو إدخاله.
  2. إدخال رقم الفاتورة.
  3. إدخال قيمة الفاتورة.
  4. تصوير الريسيت.
  5. تحديد GPS تلقائيًا.
  6. إرسال آمن عبر RPC.
- لا يتم احتساب الأوردر مباشرة، بل يدخل `pending_reconciliation`.

### لوحة الإدارة
- ملخص اليوم.
- أوردرات تحتاج مراجعة.
- أجهزة pending.
- رفع العملاء اليومي.
- تحليل العملاء.
- مطابقة الفواتير.
- تقارير أداء الدليفري.
- الحوافز الشهرية من يوم 26 إلى 25.

### Supabase
- لا حذف مباشر لأي بيانات.
- أي جداول قديمة يتم تسجيلها في `database_deprecation_registry`.
- أي أوردر لا يصبح محتسبًا إلا بعد المطابقة.
- الحضور والأوردرات مربوطة بجلسة وجهاز وGPS.

## خطوات التشغيل المقترحة
1. ارفع الكود الجديد على GitHub.
2. نفّذ Migration الجديد في Supabase:
   `supabase/52_delivery_intelligence_hardening.sql`
3. جرّب تسجيل دخول دليفري.
4. جرّب الحضور من جهاز حقيقي وليس الكمبيوتر لأن GPS مطلوب.
5. جرّب تسجيل أوردر بصورة ريسيت.
6. جرّب رفع ملف عملاء من صفحة `/admin/customer-import`.
7. افتح `/admin/customer-analytics` لمراجعة تحليل العملاء.
8. بعد التأكد، اربط النشر على Vercel.

## ملاحظات مهمة جدًا
- لم يتم حذف أي جدول أو بيانات.
- الجداول المشكوك فيها تم وضعها في سجل deprecation فقط.
- صفحة الدليفري الآن أكثر صرامة؛ لو GPS أو الريسيت غير متاح، لن يتم تسجيل الأوردر.
- بعض مشاريع Supabase القديمة قد تحتاج ضبط أسماء جداول الحضور إذا كان الجدول النهائي ليس `attendance`.
