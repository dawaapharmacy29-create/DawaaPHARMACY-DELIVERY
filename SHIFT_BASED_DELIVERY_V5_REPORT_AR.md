# تقرير نسخة V5 — نظام الشيفت الحقيقي للدليفري

## الهدف
حل مشكلة الدليفري الذي يعمل من 9 مساءً إلى 4 فجرًا بحيث لا تنقسم أوردراته بين يومين، بل تُحسب كلها على يوم بداية الشيفت.

مثال:

- بدأ الشيفت: يوم 20 الساعة 9 مساءً
- أنهى الشيفت: يوم 21 الساعة 4 فجرًا
- سجل 15 أوردر

يظهر في التقرير الشهري:

- يوم الشغل: 20
- عدد الأوردرات: 15
- عدد الساعات: 7

## أهم التعديلات

1. اعتماد `delivery_attendance` كجدول الشيفت الرسمي.
2. إضافة `attendance_id` و `work_date` إلى `delivery_orders`.
3. إضافة `attendance_id` و `work_date` إلى `internal_trips`.
4. تعديل RPC الحضور والانصراف `rider_check_in_out` بحيث يفتح شيفت واحد مستمر حتى بعد 12 بالليل.
5. تعديل RPC تسجيل الأوردر `rider_create_order` بحيث يربط كل أوردر بالشيفت المفتوح.
6. تعديل بيانات الداشبورد `rider_get_dashboard_data` بحيث تعتمد على `work_date` وليس اليوم التقويمي فقط.
7. إضافة View جديد: `rider_shift_daily_summary` لتقرير اليوم/الشيفت.
8. تحديث `delivery_rider_cycle_scorecard` ليعتمد على `work_date`.
9. تعديل واجهة الدليفري: زر "بدء الشيفت" و"إنهاء الشيفت"، ومنع تسجيل أوردر/مشوار قبل بدء الشيفت.
10. تعديل صفحة تفاصيل الدليفري الإدارية لحساب الساعات من `delivery_attendance.total_minutes`.

## ملف SQL المطلوب تشغيله

شغّل الملف التالي بعد رفع النسخة:

```text
supabase/55_shift_based_delivery_accounting.sql
```

## اختبار سريع بعد تشغيل SQL

```sql
select *
from public.rider_shift_daily_summary
order by work_date desc, rider_name;
```

```sql
select *
from public.delivery_rider_cycle_scorecard
order by total_orders desc;
```

## ملاحظات مهمة

- الأيام القديمة التي لا يوجد لها حضور فعلي ستظل محسوبة بـ `work_date` حسب قاعدة قبل 9 صباحًا = يوم أمس.
- من تاريخ تشغيل V5، الحساب الأدق سيكون من زر بداية/نهاية الشيفت.
- لا يتم إنهاء الجلسة عند 12 بالليل.
- آخر الشهر يمكن حساب الراتب من الأساسي + الساعات + الأوردرات + المشاوير + الحوافز - الخصومات.
