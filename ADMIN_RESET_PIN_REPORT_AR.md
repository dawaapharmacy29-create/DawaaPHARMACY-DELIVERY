# تقرير تعديل إدارة PIN الدليفري

## الملخص
تم إضافة طريقة آمنة لمدير النظام/مدير الفرع لإعادة تعيين PIN الدليفري من داخل صفحة **حسابات الدليفري**.

## ما تم تنفيذه
- لا يتم عرض PIN الحالي إذا كان مشفرًا.
- المدير يستطيع تعيين PIN جديد مؤقت.
- الرقم الجديد يظهر مرة واحدة فقط بعد التعيين.
- يتم حفظ PIN في قاعدة البيانات مشفرًا باستخدام `pgcrypto`.
- يتم مسح `pin_plain` بعد الحفظ.
- يتم تسجيل عملية إعادة التعيين في جدول تدقيق `rider_pin_reset_audit`.
- يتم إرسال إشعار للمندوب إذا كان جدول `notifications` موجودًا.

## الملف المطلوب تشغيله في Supabase
شغّل الملف التالي مرة واحدة:

`supabase/38_admin_reset_rider_pin.sql`

## مكان التعديل في التطبيق
- `/admin/rider-accounts`
- زر المفتاح بجانب كل دليفري يفتح نافذة تعيين PIN جديد.

## الاختبار
تم تشغيل:

```bash
npm run typecheck
npm run build
```

والبناء نجح.
