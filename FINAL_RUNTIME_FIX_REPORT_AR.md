# تقرير إصلاح النسخة التشغيلية للدليفري

## الهدف
إصلاح المشاكل التي ظهرت في التجربة الحالية قبل تشغيل الدليفري تجريبيًا على التليفون:

- أزرار إدارة الدليفري: تعديل / الأداء / المواعيد لا تفتح أي شيء.
- فشل حفظ الأوردر بسبب عمود ناقص في قاعدة البيانات مثل `receipt_ocr_confidence`.
- الحاجة إلى تثبيت تسجيل الأوردرات والمشاوير بدون فقد بيانات قديمة.
- تجهيز أساس التنبيهات والسياسات.
- تجهيز ملخصات لكل دليفري: أوردرات 1، أوردرات 1.5، الفاشلة، المشاوير.

## ما تم تنفيذه

### 1. إصلاح أزرار إدارة الدليفري
تم تعديل صفحة:

`src/pages/admin/Riders.tsx`

والأزرار أصبحت تعمل:

- **تعديل**: يفتح نافذة تعديل فعلية لبيانات المندوب.
- **الأداء**: يفتح ملخص أداء المندوب خلال دورة الشهر الحالية.
- **المواعيد**: يفتح جدول مواعيد المندوب ويمكن تعديل المواعيد وحفظها.
- **موقف/مكافأة**: يفتح نموذج تسجيل موقف أو مكافأة تحت مراجعة المدير العام.

### 2. إضافة صفحة مراجعة المواقف
تم إضافة صفحة:

`src/pages/admin/RiderActions.tsx`

وتم ربطها في:

`src/App.tsx`

على المسار:

`/admin/rider-actions`

كما تمت إضافة زر في لوحة الإدارة يفتح صفحة مراجعة المواقف والخصومات والمكافآت.

### 3. منع فشل حفظ الأوردر بسبب أعمدة ناقصة
تم تعديل حفظ الأوردر في:

`src/pages/rider/RiderDashboard.tsx`

وإضافة حفظ مرن. لو Supabase رجع خطأ بسبب عمود اختياري غير موجود في schema cache مثل:

`receipt_ocr_confidence`

التطبيق سيحاول الحفظ مرة أخرى بدون العمود الاختياري بدل أن يفشل الأوردر بالكامل.

هذا لا يغني عن تشغيل SQL، لكنه يمنع تعطيل الدليفري أثناء تحديث schema.

### 4. SQL شامل للتشغيل
تم إضافة ملف:

`supabase/30_mobile_runtime_schema_and_rider_controls_fix.sql`

الملف يجهز:

- delivery_orders بكل أعمدة الأوردرات والـ OCR والمراجعة.
- internal_trips بكل أعمدة إثبات المشاوير.
- rider_schedules لزر المواعيد.
- rider_shift_actions للفت النظر والخصومات والمكافآت.
- rider_notifications كأساس للتنبيهات.
- delivery_policies كأساس لسياسات الدليفري.
- rider_cycle_audit_summary لملخصات المندوبين.

### 5. الملخصات المطلوبة لكل دليفري
تم تجهيز View في قاعدة البيانات:

`rider_cycle_audit_summary`

ويعرض لكل مندوب:

- إجمالي الأوردرات.
- أوردرات محتسبة 1.
- أوردرات محتسبة 1.5.
- أوردرات 1.5 تحت المراجعة.
- أوردرات فاشلة.
- فواتير مكررة.
- أوردرات محذوفة.
- إجمالي المشاوير.
- مشاوير معتمدة.
- مشاوير معلقة.

## مهم جدًا
تحديث التطبيق لا يمسح أوردرات أمس. البيانات موجودة في Supabase طالما لم يتم تشغيل DELETE أو TRUNCATE. ملفات SQL الجديدة لا تحتوي على حذف بيانات.

لو الأوردرات لا تظهر، غالبًا السبب فلتر التاريخ أو الحالة، وليس أن البيانات اتمسحت.

## الاختبار
تم تشغيل:

```bash
npm run typecheck
npm run build
```

والاختبار نجح.

## خطوات التشغيل

1. انسخ النسخة فوق المشروع.
2. شغّل SQL:

`supabase/30_mobile_runtime_schema_and_rider_controls_fix.sql`

3. ارفع التعديل:

```bash
git add .
git commit -m "fix: stabilize rider runtime and admin controls"
git push origin main
```

4. بعد Vercel Ready جرّب:

- تسجيل أوردر من التليفون.
- تسجيل أوردر بصورة ريسيت.
- الضغط على تعديل / الأداء / المواعيد في إدارة الدليفري.
- تسجيل موقف/مكافأة.
- مراجعة المواقف من `/admin/rider-actions`.

