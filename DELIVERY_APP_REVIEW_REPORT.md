# Delivery App Review Report

## الملخص
تم إجراء مراجعة أمنية ومنطقية على تدفق تسجيل الدخول، الأوردرات، المشاوير، الحضور، GPS، والواجهة، مع تطبيق تحسينات آمنة على المنطق الحالي دون حذف جداول أو تغيير أسماء الأعمدة.

## المشاكل التي وجدت
1. مشوار قديم مفتوح يمكن أن يمنع تسجيل مشوار جديد أو يسبب ارتباكًا في العرض والتقارير.
2. تسجيل الأوردرات كان يعتمد على منطق مبدئي ممتاز لكنه يحتاج رسائل أوضح وتسجيل تعديل واضح قبل التسليم.
3. تعديل الأوردر قبل التسليم كان يحتاج سجلًّا واضحًا وإشعارًا أفضل.
4. تسجيل المشوار لم يكن يمر عبر حماية مراجعة للمشاوير القديمة التي ظلّت مفتوحة أكثر من 12 ساعة.
5. اللوحة الإدارية تحتاج فئة واضحة للمشاوير التي تحتاج مراجعة وأزرار إغلاق/اعتماد/رفض يدوي.

## الملفات التي تم تعديلها
- src/lib/delivery.ts
  - أضيفت دوال للتحقق من المشاوير القديمة وتدويرها للمراجعة.
  - أضيفت دالة لتسجيل تعديلات الأوردر في delivery_order_edit_logs عند توفر الجدول.
- src/pages/rider/RiderDashboard.tsx
  - قبل حفظ مشوار جديد، يتم التحقق من وجود مشوار قديم مفتوح وإرساله للمراجعة عند موافقة المستخدم.
  - بعد تعديل الأوردر قبل التسليم، يتم تسجيل التعديلات في edit log مع تحديث البيانات فورًا.
- src/pages/admin/Trips.tsx
  - أضيفت فلتر "يحتاج مراجعة" وواجهة إدارية لإغلاق/اعتماد/رفض المشوار يدويًا.

## سبب كل تعديل
- حماية المشاوير القديمة: تمنع التلاعب وتضمن أن المشوار القديم لا يبقى مفتوحًا إلى الأبد.
- تسجيل التعديلات: يحافظ على شفافية التعديلات قبل التسليم ويُسهل المراجعة الإدارية.
- واجهة الإدارة: تساعد المدير على اتخاذ قرار سريع على المشاوير التي تحتاج مراجعة.

## المشاكل التي تم إصلاحها
- تم إضافة حماية واضحة لبدء مشوار جديد عند وجود مشوار قديم مفتوح.
- تم السماح للدليفري بالاختيار بين إرسال المشوار القديم للمراجعة أو إلغاء العملية.
- تم تسجيل تعديلات الأوردر في سجل مخصص قبل التسليم.
- تم تحسين واجهة الإدارة للمشاوير التي تحتاج مراجعة.
- تم الحفاظ على عدم كسر تسجيل الدخول أو تسجيل الأوردرات أو الحضور.

## المشاكل التي تحتاج قرار إداري
1. ما إذا كان سيتم اعتماد استخدام جدول delivery_trips أو العمل على جدول internal_trips الحالي.
2. ما إذا كان سيتم إضافة RPC أو migration إضافية لتوسيع الحقول: started_at, ended_at, return_lat, return_lng, return_accuracy, manual_return_reason.
3. ما إذا كان سيتم تمكين RLS أو شغل سياسات موحدة على الجداول الجديدة في Supabase.

## طريقة الاختبار
تم تنفيذ ما يلي:
- npm run typecheck
- npm run build

## SQL Migration المطلوب (اختياري/مقترح)
إذا كانت بيئة Supabase تحتوي على جدول delivery_trips فعليًا وتريد دعم كامل للمشوار، يُفضّل تشغيل migration لاحقًا لإضافة الأعمدة التالية عند الحاجة:

```sql
alter table public.delivery_trips
  add column if not exists started_at timestamptz,
  add column if not exists ended_at timestamptz,
  add column if not exists return_lat double precision,
  add column if not exists return_lng double precision,
  add column if not exists return_accuracy double precision,
  add column if not exists manual_return_reason text,
  add column if not exists review_reason text,
  add column if not exists needs_review boolean default false;
```

## ملاحظات مهمة
- لا تم حذف أو تغيير بيانات أو أسماء جداول موجودة.
- لا تم تغيير منطق تسجيل الدخول الأساسي.
- لا تم تغيير منطق الأوردرات الأساسية إلا مع إضافة حماية آمنة وملفات سجل.
