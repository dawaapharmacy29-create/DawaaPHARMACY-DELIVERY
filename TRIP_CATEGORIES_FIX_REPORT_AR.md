# تقرير تعديل أنواع المشاوير وجهات المشوار

## ما تم تنفيذه

تم تعديل صفحة الدليفري `/rider` في نموذج **سجل مشوار** ليصبح أكثر وضوحًا وتنظيمًا.

### 1. أنواع المشاوير الجديدة
تمت إضافة الاختيارات التالية:

- بين الفروع
- مخزن
- مستلزمات
- صيدلية
- استلام شحن
- إكسسوار
- أخرى

### 2. منطق الاختيارات حسب نوع المشوار

#### بين الفروع
عند اختيار **بين الفروع** يظهر:

- من أي فرع؟
- إلى أي فرع؟

والاختيارات:

- فرع الشامي
- فرع شكري
- فرع بسيسة
- فرع زكريا
- فرع المنشية

#### مخزن
عند اختيار **مخزن** يظهر:

- خارج من: قائمة الفروع
- رايح إلى مخزن: قائمة مخازن بكود

المخازن المضافة:

- 1 - مخزن المعداوي
- 2 - مخزن سونيستا
- 3 - مخزن الحياة
- 4 - مخزن المحلة
- 5 - المخزن الرئيسي
- 6 - المكتب

مع اختيار: **إضافة مخزن جديد**.

#### مستلزمات
يظهر:

- خارج من: قائمة الفروع
- جهة المستلزمات: قائمة + إضافة جهة جديدة

#### صيدلية
يظهر:

- خارج من: قائمة الفروع
- اسم الصيدلية: حقل كتابة يدوي

#### استلام شحن
يظهر:

- مكان / شركة الشحن
- التسليم إلى: قائمة الفروع

#### إكسسوار
يظهر:

- خارج من: قائمة الفروع
- مخزن الإكسسوار: قائمة + إضافة مخزن جديد

### 3. تحسين الحفظ
تم تعديل حفظ المشوار ليحفظ:

- `trip_type`
- `from_label`
- `to_label`
- `reason`
- `related_invoice_number`
- `rider_name`
- `branch_name`
- `trip_rate`
- `trip_earning`

كما تمت إضافة رسائل خطأ أوضح عند فشل الحفظ.

### 4. ملفات SQL
تمت إضافة ملف:

`supabase/22_trip_categories_destinations_fix.sql`

وظيفته:

- تجهيز جدول `internal_trips` لو غير موجود.
- إضافة الأعمدة الناقصة.
- دعم أنواع المشاوير الجديدة.
- إصلاح RLS مؤقتًا بما يناسب نظام PIN الحالي.

## الاختبارات

تم تشغيل:

```bash
npm run typecheck
npm run build
```

والاختبارين نجحوا.

## ملاحظات مهمة قبل التجربة

1. شغّل SQL:

`supabase/22_trip_categories_destinations_fix.sql`

في Supabase SQL Editor.

2. انسخ ملفات التطبيق المعدلة.

3. اعمل commit و push.

4. بعد Vercel Ready، جرّب من `/rider`:

- سجل مشوار
- اختار بين الفروع
- اختار مخزن
- اختار صيدلية
- اختار استلام شحن
- احفظ المشوار

