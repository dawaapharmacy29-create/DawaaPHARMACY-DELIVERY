import { useCallback, useEffect, useState } from 'react'
import { AlertTriangle, CheckCircle2, LogIn, RefreshCw, Stethoscope } from 'lucide-react'
import { toast } from 'sonner'
import { supabase } from '../lib/supabase'
import { getRiderSession } from '../lib/auth'

type LiveRow = {
  account_id: string; username: string; rider_id: string; rider_name: string; branch_name?: string | null
  active_sessions_count: number; latest_session_at?: string | null; login_status: string
  open_attendance_id?: string | null; shift_date?: string | null; check_in_time?: string | null
  shift_status: 'shift_open' | 'no_open_shift' | 'offline'; current_shift_orders: number; current_shift_trips: number; last_order_at?: string | null
}

export default function RiderOperationsHealth() {
  const [rows, setRows] = useState<LiveRow[]>([]); const [missingOrders, setMissingOrders] = useState(0); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState<string | null>(null)
  const load = useCallback(async () => { setLoading(true); const [live, missing] = await Promise.all([supabase.from('rider_live_status_view').select('*').order('rider_name'), supabase.from('delivery_orders_missing_shift_view').select('id', { count: 'exact', head: true })]); if (live.error) { setRows([]) } else setRows((live.data || []) as LiveRow[]); setMissingOrders(missing.count || 0); setLoading(false) }, [])
  useEffect(() => { void load() }, [load])
  async function action(name: 'open' | 'cleanup', riderId?: string) { const token = getRiderSession().session_token; if (!token) return toast.error('الإجراء يحتاج جلسة مدير PIN صالحة'); setBusy(`${name}-${riderId || 'all'}`); const { data, error } = name === 'open' ? await supabase.rpc('admin_open_rider_shift', { p_token: token, p_rider_id: riderId }) : await supabase.rpc('admin_close_old_rider_sessions', { p_token: token, p_rider_id: riderId || null }); const result = Array.isArray(data) ? data[0] : data; if (error || !result?.success) toast.error(error?.message || result?.message || 'تعذر تنفيذ الإجراء'); else { toast.success(result.message); await load() } setBusy(null) }
  const logged = rows.filter(r => r.login_status === 'logged_in'), open = rows.filter(r => r.shift_status === 'shift_open'), noShift = rows.filter(r => r.shift_status === 'no_open_shift'), duplicateSessions = rows.filter(r => Number(r.active_sessions_count) > 1)
  return <section className="rounded-[1.8rem] border border-slate-100 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-teal-50 text-teal-700"><Stethoscope/></span><div><h2 className="font-black text-[#102a32]">صحة تشغيل الدليفري</h2><p className="mt-1 text-xs font-bold text-slate-400">Login، الشيفت، الجلسات، والأوردرات غير المرتبطة</p></div></div><div className="flex gap-2"><button onClick={() => void action('cleanup')} disabled={!!busy} className="rounded-xl bg-amber-50 px-3 py-2 text-xs font-black text-amber-700">إغلاق الجلسات القديمة</button><button onClick={() => void load()} className="rounded-xl border p-2 text-slate-500"><RefreshCw size={17} className={loading ? 'animate-spin' : ''}/></button></div></div><div className="mt-4 grid grid-cols-2 gap-2 lg:grid-cols-5"><Health label="مسجلون الآن" value={logged.length} icon={<LogIn/>}/><Health label="داخل شيفت" value={open.length} tone="green" icon={<CheckCircle2/>}/><Health label="Login بلا شيفت" value={noShift.length} tone="red" icon={<AlertTriangle/>}/><Health label="Sessions مكررة" value={duplicateSessions.length} tone="amber" icon={<AlertTriangle/>}/><Health label="أوردرات بلا شيفت" value={missingOrders} tone="amber" icon={<AlertTriangle/>}/></div><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[960px] text-sm"><thead className="bg-slate-50 text-[10px] font-black text-slate-500"><tr><th className="p-3 text-right">الدليفري</th><th>الحالة</th><th>Sessions</th><th>بداية الشيفت</th><th>أوردرات</th><th>مشاوير</th><th>آخر Login</th><th>آخر أوردر</th><th>إجراء</th></tr></thead><tbody>{rows.map(r => <tr key={r.account_id} className="border-t border-slate-50"><td className="p-3"><b>{r.rider_name}</b><p className="text-[10px] text-slate-400">{r.username} · {r.branch_name || '—'}</p></td><td><span className={`rounded-full px-2 py-1 text-[10px] font-black ${r.shift_status === 'shift_open' ? 'bg-emerald-50 text-emerald-700' : r.shift_status === 'no_open_shift' ? 'bg-rose-50 text-rose-700' : 'bg-slate-100 text-slate-500'}`}>{r.shift_status === 'shift_open' ? 'داخل شيفت' : r.shift_status === 'no_open_shift' ? 'Login بلا شيفت' : 'غير متصل'}</span></td><td className={Number(r.active_sessions_count) > 1 ? 'font-black text-rose-600' : 'font-black'}>{r.active_sessions_count}</td><td>{r.check_in_time ? new Date(r.check_in_time).toLocaleTimeString('ar-EG', { hour: '2-digit', minute: '2-digit' }) : '—'}</td><td>{r.current_shift_orders || 0}</td><td>{r.current_shift_trips || 0}</td><td className="text-xs">{r.latest_session_at ? new Date(r.latest_session_at).toLocaleString('ar-EG') : '—'}</td><td className="text-xs">{r.last_order_at ? new Date(r.last_order_at).toLocaleString('ar-EG') : '—'}</td><td><div className="flex gap-1">{r.shift_status === 'no_open_shift' && <button onClick={() => void action('open', r.rider_id)} disabled={!!busy} className="rounded-lg bg-teal-600 px-2 py-1 text-[10px] font-black text-white">فتح شيفت</button>}<button onClick={() => void action('cleanup', r.rider_id)} disabled={!!busy} className="rounded-lg bg-slate-100 px-2 py-1 text-[10px] font-black">مزامنة</button></div></td></tr>)}</tbody></table>{!loading && !rows.length && <p className="py-8 text-center text-xs font-bold text-slate-400">طبّق Migration 57 لإظهار صحة التشغيل</p>}</div></section>
}

function Health({ label, value, icon, tone = 'slate' }: { label: string; value: number; icon: React.ReactNode; tone?: 'slate' | 'green' | 'amber' | 'red' }) { const cls = { slate: 'bg-slate-50 text-slate-600', green: 'bg-emerald-50 text-emerald-700', amber: 'bg-amber-50 text-amber-700', red: 'bg-rose-50 text-rose-700' }[tone]; return <div className={`rounded-2xl p-3 ${cls}`}><div className="flex items-center gap-2 text-[10px] font-black">{icon}{label}</div><p className="mt-2 text-2xl font-black">{value}</p></div> }
