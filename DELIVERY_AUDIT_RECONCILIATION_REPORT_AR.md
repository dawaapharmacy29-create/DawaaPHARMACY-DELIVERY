# تقرير تعديل منع التلاعب ومطابقة بي كونكت

## الهدف
تحويل التطبيق من مجرد تسجيل أوردرات إلى نظام مراجعة قوي:
- الأوردر الفاشل يظهر في البيان لكنه لا يُحتسب للدليفري.
- الفاتورة لا تُحتسب شهريًا إلا بعد مطابقتها مع ملف بي كونكت.
- الفواتير المكررة تظهر للمراجعة ولا تُحتسب تلقائيًا.
- أوردرات ×1.5 تظهر كحالات مراجعة خاصة، بدون حساب راتب نهائي داخل التطبيق.

## ما تم تعديله

### صفحة الدليفري `/rider`
- عند تسجيل أوردر جديد يتم حفظه كـ `pending_reconciliation` وليس كأوردر محتسب نهائيًا.
- عند ضغط “فشل” على الأوردر يتم تحديثه إلى:
  - `status = failed`
  - `is_countable = false`
  - `final_count_status = excluded_failed`
  - `count_exclusion_reason = failed_order`
  - `order_earning = 0`
- إضافة عدادات واضحة:
  - فاشلة لا تحتسب
  - قيد المراجعة
  - أوردرات معتمدة بعد المطابقة

### صفحة مطابقة بي كونكت `/admin/reconciliation`
- أصبحت تقرأ Excel/CSV من بي كونكت باستخدام SheetJS.
- تطابق رقم الفاتورة مع تسجيلات الدليفري في الدورة الحالية من يوم 26 إلى يوم 25.
- تصنف النتائج إلى:
  - محتسبة بعد المطابقة
  - فاشلة لا تحتسب
  - غير موجودة في بي كونكت
  - مكررة وتحتاج مراجعة
  - ×1.5 وتحتاج مراجعة قيمة
  - فواتير موجودة في بي كونكت ولم يسجلها أي دليفري

## ملف SQL الجديد
شغل الملف التالي في Supabase:

`supabase/24_delivery_audit_reconciliation_fix.sql`

## ملاحظات مهمة
- التطبيق لا يحسب الراتب النهائي الآن.
- الحساب النهائي يتم من تقرير المطابقة والمراجعة الإدارية.
- الفاشل لا يدخل في العدد النهائي حتى لو ظهر في شاشة الدليفري.
- أي فاتورة غير موجودة في بي كونكت تعتبر مستبعدة/مشكوك فيها حتى تعتمد يدويًا.

## اختبار مقترح
1. سجل أوردر من الدليفري.
2. اجعله فاشلًا.
3. افتح ملخص المراجعة وتأكد أنه ظهر ضمن الفاشل ولا يحتسب.
4. ارفع ملف بي كونكت من صفحة المطابقة.
5. راجع الأرقام: المحتسب، الفاشل، غير الموجود، المكرر، ×1.5.
