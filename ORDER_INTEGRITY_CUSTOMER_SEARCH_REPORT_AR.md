# تقرير تعديل تسجيل الأوردر ومنع التلاعب

## المنفذ
تم تعديل صفحة الدليفري `/rider` لتصبح عملية تسجيل الأوردر أدق وأقوى للمراجعة الإدارية، بدون حساب راتب نهائي تلقائي.

## أهم التعديلات

1. **بحث العملاء**
   - البحث بالاسم أو الكود أو رقم الهاتف.
   - دعم علامة `*` كـ wildcard في البحث.
   - إضافة زر بحث واضح بجوار خانة العميل.

2. **بيانات العميل أصبحت في خانات مستقلة**
   - اسم العميل.
   - كود العميل.
   - رقم الهاتف.
   - عنوان تسليم الأوردر الحالي.
   - يمكن تعديل العنوان حتى لو العميل مسجل بعنوان آخر.

3. **أوردر ×1.5**
   - إضافة اختيار واضح لأوردر يستحق مراجعة ×1.5.
   - إجبار كتابة سبب واضح عند اختيار ×1.5.
   - لا يتم حساب راتب تلقائي، بل يتم حفظ العلامة للمراجعة.

4. **منع تكرار الفاتورة**
   - عند وجود نفس رقم الفاتورة سابقًا، يظهر تحذير استثنائي.
   - لا يتم حفظ التكرار إلا بسبب واضح.
   - يتم تسجيل اسم الدكتور الذي حضّر/طلع الأوردر.

5. **ملخص التسجيلات بدل المستحقات النهائية**
   - تم تغيير كارت المستحقات إلى ملخص تسجيلات تحت المراجعة.
   - لا يتم عرض مستحق مالي نهائي داخل صفحة الدليفري.

## ملف SQL المطلوب تشغيله
`supabase/23_order_integrity_customer_search_fix.sql`

## الاختبار
- `npm run typecheck` نجح.
- `npm run build` نجح.
